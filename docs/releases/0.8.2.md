# Hermes Finance 0.8.2 — maintenance release record

> **Status:** PUBLISHED
> **Release:** `0.8.2`
> **Canonical issue:** #309
> **Permanent release control:** #124
> **Release source:** canonical `main`
> **Released tag object:** `bfa1194d4151bb72882f4230f144b039d240eda9`
> **Released peeled commit:** `a22542d7b20ebdf34e38384004162d409f163ab3`
> **Published:** `2026-09-05`
> **Public notes:** `docs/release-notes-0.8.2.md`
> **Previous release-prep record:** `docs/releases/0.8.1.md`

This is the published maintenance release after Stable `v0.8.1`. It documents
already integrated maintenance work and does not change product behavior,
financial formulas, provider semantics, schema semantics or runtime data.

## Included maintenance

- **#298 — launcher Stable release lifecycle:** failed launcher self-update
  experiment; closed `not_planned` after owner UAT. The safety work remains
  historical evidence, not a proven canonical update flow. See #313.
- **#299 — launcher process ownership:** restart-safe process ownership and Stop recovery.
- **#302 — launcher visual/content polish:** canonical owner-facing Stable/Preview identity and final presentation polish.
- **#142 — Tax/IIS Planner Lite:** current-state backend and owner UI; projection expansion is not part of this release.
- **#143 — deterministic insights:** Deterministic Financial Insights Engine v1 is integrated with AI Analysis Bundle schema `1.2.0` from persisted evidence, without LLM/cloud calls; the dedicated Insights UI remains deferred.

## Version metadata

The canonical runtime/package version is `0.8.2` in backend package metadata and `__version__`, generated `backend/uv.lock`, frontend package metadata and `frontend/package-lock.json`, `/api/health` expectations, release-verification assertions, Windows launcher identity/config/tests, `CHANGELOG.md`, `README.md`, the Project Wiki, execution history, this record and the public notes.

The release helper remains the guarded publication path for future releases:

~~~powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\release.ps1 `
  -Version 0.8.2 `
  -ExpectedMainSha <exact-accepted-origin-main-sha> `
  -ReleaseNotes .\docs\release-notes-0.8.2.md
~~~

The tag and GitHub Release for `0.8.2` were created through the guarded release
control on #124. This record is not a request to publish another release.

## Preserved safety contract

- local single-user Windows-first runtime, loopback-only at `127.0.0.1:8000`;
- no cloud, auth, telemetry, trading or provider write operations;
- provider, network and file-processing actions remain explicit owner actions;
- no automatic upload or background provider refresh;
- closed months remain immutable until explicit Reopen;
- unknown/unavailable financial evidence remains explicit and is not silently treated as zero;
- canonical Alembic head remains exactly `0036_broker_baseline_provenance`; this task adds no migration;
- private Stable/Preview/runtime data, `.env`, databases, backups, exports/PDFs and credentials are not used;
- #308 remains a known non-blocking cosmetic follow-up and is not required for this release.

## Not performed

- no financial formula change, migration, provider/runtime behavior change, Scenario Lab or unrelated product scope;
- no access to Stable/Preview/runtime data;
- no launcher redesign or new product feature was included in this release.
