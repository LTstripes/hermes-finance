# Hermes Finance 1.0.0 — release-candidate record

> **Status:** PREPARED / UAT-PENDING
> **Canonical issue:** #480 (R10-REL01)
> **Predecessor:** published Stable `v0.9.0`
> **Permanent release control:** #124
> **Public notes:** `docs/release-notes-1.0.0.md`

This record describes the prepared `1.0.0` candidate. It is deliberately not a publication record: no `v1.0.0` tag or GitHub Release is created by release preparation, and production Stable remains `v0.9.0` until Owner OPS03 Preview/UAT passes and guarded publication completes.

## Candidate boundary

The candidate packages accepted canonical work integrated after `v0.9.0`, centered on:

- cohesive UI v2 completion and exact-SHA Owner UAT;
- controlled default switch with `/` -> v2 and durable `/v1` rollback;
- complete Data/App owner surface;
- restore outcome truthfulness and ambiguity safety;
- protected recovery-point publisher;
- bounded verified retention from #460 / PR #473;
- accepted CI/release/process hardening already present on canonical main.

Release preparation itself adds no financial formula, schema, provider, runtime-data or network behavior. #461 DR rehearsal, #462 legacy post-restore state reload, #476 browser-gate work and any future v1 retirement remain outside this candidate.

## Version identity

The prepared application identity is `1.0.0` across backend/frontend package metadata, generated lock metadata, `/api/health` expectations, current release-verification assertions and synthetic current-version fixtures.

Historical `0.9.0` release records remain unchanged where they describe the actually published predecessor.

## Owner gate and publication boundary

After release-prep integration, record one exact canonical candidate SHA and pin OPS03 Preview/UAT to that SHA using isolated UAT data.

An Owner `PASS` permits guarded #124 publication from the same exact canonical code identity.

Only after publication should OPS02 perform the real Stable transition:

`v0.9.0 -> v1.0.0`

No owner/private production data, Stable mutation, release tag or GitHub Release is part of this preparation task.
