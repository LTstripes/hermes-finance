# Hermes Finance 0.8.1 — maintenance release record

> **Status:** RELEASE PREP
> **Release:** `0.8.1`
> **Canonical issue:** #295
> **Exact baseline `origin/main`:** `d04f46696a991ea59066b59d4870980ac4b69089`
> **Task branch:** `m08-01-release-081`
> **Release-prep date:** `2026-09-04`
> **Target tag after integration:** `v0.8.1`
> **Public notes:** `docs/release-notes-0.8.1.md`
> **Previous release record:** `docs/releases/0.8.0.md`

This is a release-prep-only maintenance release on the published 0.8.0 state. It synchronizes repository version identity and documents already integrated maintenance work. It does not change product behavior, financial formulas, provider semantics, schema semantics or runtime data. Publication (`main` merge, tag, GitHub Release) is a separate guarded owner/integrator step.

## Included maintenance

- **#277 — Preview update from launcher:** launcher-owned update of Preview from canonical `main`, preserving explicit owner action and Stable/Preview separation.
- **#278 — one-click prepare/repair/start:** launcher-owned dependency/runtime preparation and recovery for the normal owner workflow; ordinary Start remains offline.
- **#279 — Stable/Preview identity and owner workflow:** clearer released-versus-unreleased identity and owner-facing primary action state.
- **#284 — launcher visual/layout polish:** desktop layout, spacing, readability and responsive behavior without safety-semantic changes.
- **#285 — AI/export fact quality:** clearer salary calculated-versus-actual semantics, valuation freshness versus position completeness, explicit unavailable states, warning deduplication, IIS coverage, and additive AI Analysis Bundle schema `1.1.0` while preserving portfolio-review package `1.0.0`.
- **#280 — safe workspace cleanup:** repository-owned cleanup tooling with fail-closed protection for runtime and private-data boundaries.
- **#282 / #292 — verification infrastructure:** backend CI parallel lanes with slow-test telemetry and a deterministic quote-freshness regression test with a pinned clock; neither is a user financial feature.

## Version metadata

The canonical runtime/package version is `0.8.1` in backend package metadata and `__version__`, generated `backend/uv.lock`, frontend package metadata and `frontend/package-lock.json`, `/api/health` expectations, release-verification assertions, Windows launcher identity/config/tests, `CHANGELOG.md`, `README.md`, the Project Wiki, execution history, this record and the public notes.

The release helper remains the guarded publication path:

~~~powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\release.ps1 `
  -Version 0.8.1 `
  -ExpectedMainSha <exact-accepted-origin-main-sha> `
  -ReleaseNotes .\docs\release-notes-0.8.1.md
~~~

This task does not create the target tag or GitHub Release.

## Preserved safety contract

- local single-user Windows-first runtime, loopback-only at `127.0.0.1:8000`;
- no cloud, auth, telemetry, trading or provider write operations;
- provider, network and file-processing actions remain explicit owner actions;
- no automatic upload or background provider refresh;
- closed months remain immutable until explicit Reopen;
- unknown/unavailable financial evidence remains explicit and is not silently treated as zero;
- canonical Alembic head remains exactly `0036_broker_baseline_provenance`; this task adds no migration;
- private Stable/Preview/runtime data, `.env`, databases, backups, exports/PDFs and credentials are not used.

## Not performed

- #143 / Deterministic Financial Insights Engine was not touched or started;
- no product-function refactor, schema change or provider behavior change;
- no access to Stable/Preview/runtime data;
- no PR, merge to `main`, `v0.8.1` tag or GitHub Release.
