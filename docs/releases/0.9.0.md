# Hermes Finance 0.9.0 — published release record

> **Status:** PUBLISHED / OWNER UAT PASS
> **Canonical release-prep issue:** #408 (R09-REL01)
> **Runtime redesign parent:** #313
> **Permanent release control:** #124
> **Public notes:** `docs/release-notes-0.9.0.md`
> **Published:** 2026-09-17
> **Release source / owner-UAT SHA:** `c90a842ec5e85fc5ac0de4aedd5d7fd14c09ae36`
> **Annotated tag object:** `07c06d44f8b780e721be346a21909ca02585d57d`

Hermes Finance `v0.9.0` is the first release to complete the redesigned composable owner runtime/release lifecycle on a real production transition.

The published annotated tag peels exactly to the same code identity that passed owner OPS03 Preview/UAT:

`c90a842ec5e85fc5ac0de4aedd5d7fd14c09ae36`

Guarded Release #253 / run `35235369797` completed successfully. The GitHub Release is published with `draft=false` and `prerelease=false`.

## Included product/runtime state

The release contains the accepted work integrated after `v0.8.2`, including:

- Decision Support v1 and Scenario Lab;
- Performance v1 with exact portfolio/account XIRR and TWRR;
- bounded PERF04A/B/C account/internal-transfer decomposition without unsupported instrument-level attribution;
- linked-financing integrity and AI financial-review/export hardening;
- OPS01 explicit Prepare + deterministic Start;
- OPS02 explicit immutable Stable update;
- OPS03 exact-SHA isolated Preview/UAT preparation;
- CI/release-safety improvements.

UI v2 remains a separate workstream and is not part of `v0.9.0`.

Canonical Alembic head is `0041_debt_linked_account`; release preparation itself added no migration or financial semantics.

## Owner OPS03 Preview/UAT — PASS

Owner tested an isolated Preview pinned exactly to:

`c90a842ec5e85fc5ac0de4aedd5d7fd14c09ae36`

The Preview used an isolated verified copy of owner data. Exact SHA pinning, application health/version, product behavior and production-data isolation passed.

## First real OPS02 Stable transition — PASS

Real owner transition:

`v0.8.2 -> v0.9.0`

Evidence:

- source Stable HEAD: `a22542d7b20ebdf34e38384004162d409f163ab3` / `v0.8.2`;
- verified pre-mutation backup id: `finance_backup_20260917T144656192481Z`;
- target Stable HEAD: `c90a842ec5e85fc5ac0de4aedd5d7fd14c09ae36`;
- local `v0.9.0^{}` peeled commit: exact same SHA;
- production DB hash unchanged by the update operation before explicit Start;
- target Prepare + Validate passed;
- no auto-start or DB migration occurred during OPS02.

## Production v0.9.0 Start — PASS

After OPS02, owner explicitly started Stable `v0.9.0` on the production database.

- production readiness smoke: PASS;
- `/api/health`: `status=ok`, `version=0.9.0`;
- owner data continuity: PASS.

This completes the real-world acceptance boundary that was intentionally left open when OPS02 was first integrated.

## Runtime architecture after this release

- launcher: owner-facing profile/status/Start/Stop shell;
- Prepare/Validate: `scripts/prepare-runtime.ps1`;
- deterministic Start: `scripts/start-local.ps1`;
- Stable release transition: `scripts/update-stable.ps1` from trusted control checkout;
- exact candidate Preview/UAT: `scripts/prepare-preview.ps1`;
- release publication: guarded Release Control #124.

The launcher-owned self-update experiment is not the canonical update path. The success of `v0.9.0` is the proof that these smaller operations compose correctly on a real owner release transition.

Detailed closeout: `docs/R09_RUNTIME_RELEASE_CLOSEOUT_2026-09-17.md`.
