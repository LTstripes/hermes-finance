# Hermes Finance 0.6.2 — maintenance release record

> **Status:** RELEASE PREP
> **Release:** `0.6.2`
> **Canonical issue:** #113
> **Exact baseline `origin/main`:** `382d572a2da976c76bd7dc873153dae61948c6c2`
> **Task branch:** `m06-06-release-062`
> **Release date:** `2026-08-25`
> **Target tag after integration:** `v0.6.2`
> **Public notes:** `docs/release-notes-0.6.2.md`
> **Previous release record:** `docs/releases/0.6.1.md`

This is a maintenance release on the published 0.6.1 product. It does not add a new product line, provider contract or trading semantics. Publication (`main` merge, tag, GitHub Release) is a separate guarded owner/integrator step.

## Назначение

`0.6.2` синхронизирует release identity и фиксирует уже интегрированные M06-04 / M06-05:

- M06-04 — безопасный auditable retract ошибочно применённых Alfa statement payouts; Alembic head `0029_statement_event_retract`;
- M06-05 — polish layout редактора месяца и Alfa prepared-import review.

Исторические записи 0.6.0 и 0.6.1 не переписываются.

## Что вошло с main baseline

M06-04 merge `53610ce370f70bdf028d85d97692f83b8ba79014` (PR #110 / issue #108), worker `89e816231f44fe529bd44dfdd5ae069e09bb9874`.

M06-05 integration merge `382d572a2da976c76bd7dc873153dae61948c6c2` (PR #112 / issue #109), original candidate `ac23dcc7ef150ff93d3b373ebb13a81bf4672320`, integration candidate `a04ac793550c06bc138645967922b036c76d9798`.

Exact-main CI #317 was fully green on this baseline (Backend, Frontend, Privacy guard, Windows production smoke).

## Неподвижный safety contract

- нет OCR;
- нет persistent raw Alfa/provider payload;
- нет persistent Alfa account mapping;
- Apply только по явно выбранным строкам;
- duplicate/idempotency guards;
- CLOSED/missing month fail closed;
- generic investment-flow delete не уничтожает statement provenance молча;
- retract statement-specific и auditable;
- нет изменения provider/trading семантики;
- канонический Alembic head остаётся `0029_statement_event_retract`; эта задача не добавляет новую миграцию.

## Version metadata

Canonical runtime/package version is `0.6.2` in `__version__`, backend `pyproject.toml` / `uv.lock`, frontend `package.json` / `package-lock.json`, `/api/health`, CHANGELOG and README.

Public GitHub notes for the guarded helper live in `docs/release-notes-0.6.2.md`. They are compatible with:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\release.ps1 `
  -Version 0.6.2 `
  -ExpectedMainSha <exact-accepted-origin-main-sha> `
  -ReleaseNotes .\docs\release-notes-0.6.2.md
```

The helper requires the exact accepted `origin/main` SHA. This task does not create the tag or GitHub Release.
