# Hermes Finance 0.6.1 — maintenance release record

> **Status:** RELEASE PREP
> **Release:** `0.6.1`
> **Canonical issue:** #106
> **Exact baseline `origin/main`:** `196e992c7b3a72255c7b91ca7ec11ef9e1e32281`
> **Task branch:** `m06-03-release-061`
> **Release date:** `2026-08-25`
> **Target tag after integration:** `v0.6.1`
> **Public notes:** `docs/release-notes-0.6.1.md`
> **Previous release record:** `docs/releases/0.6.0.md`

This is a maintenance release on the published 0.6.0 product. It does not add a product feature, schema, provider contract or persistence path. Publication (`main` merge, tag, GitHub Release) is a separate guarded owner/integrator step.

## Назначение

`0.6.1` синхронизирует release identity и фиксирует уже интегрированные UX-правки:

- M06-01 — плотность редактора месяца и недостающие Edit-действия;
- M06-02 — иерархия quote preview и review Alfa statement import.

Историческая запись 0.6.0 не переписывается.

## Что вошло с main baseline

M06-01 merge `a00e0768db2827bdfad917559c82aab01aea745d` (PR #103), worker `27705331cf3731b72153b79d0d194e7b9912458c`.

M06-02 merge `196e992c7b3a72255c7b91ca7ec11ef9e1e32281` (PR #105 / issue #104), worker `a78a0711d25c74f32c22982992cd9f290400e7d5`.

## Неподвижный safety contract

- нет OCR;
- нет persistent raw Alfa/provider payload;
- нет persistent Alfa account mapping;
- Apply только по явно выбранным строкам;
- duplicate/idempotency guards;
- CLOSED/missing month fail closed;
- нет изменения provider/trading семантики;
- нет новой Alembic migration.

## Version metadata

Canonical runtime/package version is `0.6.1` in `__version__`, backend `pyproject.toml` / `uv.lock`, frontend `package.json` / `package-lock.json`, `/api/health`, CHANGELOG and README.

Public GitHub notes for the guarded helper live in `docs/release-notes-0.6.1.md`. They are compatible with:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\release.ps1 `
  -Version 0.6.1 `
  -ExpectedMainSha <exact-accepted-origin-main-sha> `
  -ReleaseNotes .\docs\release-notes-0.6.1.md
```

The helper requires the exact accepted `origin/main` SHA. This task does not create the tag or GitHub Release.
