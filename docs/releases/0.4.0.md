# Hermes Finance 0.4.0 — released record

> **Status:** RELEASED
> **Release:** `0.4.0`
> **Exact tagged SHA:** `5a29afb9870304faffb9c5911d4c23bcb2563349`
> **Tag:** `v0.4.0`
> **Publication note:** `main` / `r04` statements below are publication-time history, not current repository state. `r04` is a closed historical development lineage. Current `main` need not equal `v0.4.0`.
> **Integration line at publication:** `r04`
> **Canonical baseline at the R04-09 gate:** `0fbe819f37d52e1f04e59fa0f8f6a3a85f5bbf5a`
> **Accepted implementation checkpoint after R04-08:** `5a7d0add14d1699e72993af522d2e61f47a27144`
> **Release-candidate branch at the gate:** `r04-09-grok`
> **Главная спецификация:** `docs/MASTER_SPEC.md`
> **MOEX quote semantics:** `docs/adr/0009-moex-market-identity-and-quote-semantics.md`
> **Current provider strategy:** `docs/adr/0010-market-and-broker-provider-strategy.md`
> **Execution history:** `docs/EXECUTION_HISTORY.md`

This file is a historical record of the tagged 0.4.0 line. It is not an active development plan.

## 0. Назначение и актуальная архитектура

`0.4.0` добавляет owner-triggered обновление рыночных котировок поддерживаемых инструментов с обязательным preview до применения.

**Production market-data provider для 0.4 — T-Invest API** через read-only token. Direct MOEX ISS остаётся reference/technical adapter и не является production fallback. Решение зафиксировано ADR 0010.

Каноническая market identity provider-neutral:

```text
provider + provider_instrument_id + optional provider_venue_id
```

Для T-Invest canonical provider key — `instrument_uid`; FIGI не является canonical identity. MOEX-specific `engine / market / boardid / secid` изолированы за MOEX codec.

Релиз остаётся local-only, single-user, Windows-first. Никакого cloud/auth/telemetry, background refresh или trading API.

### Неподвижные инварианты

- runtime слушает только loopback; canonical production address — `127.0.0.1:8000`;
- внешний market provider read-only;
- network access только после явного действия владельца;
- никакого silent provider fallback;
- внешняя цена сначала показывается в preview и никогда не применяется молча;
- frontend не является financial source of truth;
- closed month immutable до explicit reopen;
- mapping reference data не переписывает historical snapshots;
- `Decimal` / integer kopecks + `ROUND_HALF_UP`; никакой binary-float financial arithmetic;
- automatic NKD/FX conversion вне scope;
- private DB/seed/exports/backups/tokens/owner payloads не попадают в Git, CI или logs.

---

## 1. Branching после публикации

### Released identity

- exact tagged SHA = `5a29afb9870304faffb9c5911d4c23bcb2563349` (`v0.4.0`);
- `main` / `r04` statements in the historical task notes below describe publication-time lineage, not current repository state;
- `r04` is a closed historical development lineage;
- this file does not require current `main` to equal `v0.4.0`.

### `r04` — closed integration line `0.4.0`

- R04 is closed;
- this document is a historical task record, not an active release plan.

---

## 2. Актуальный backlog 0.4

| ID | Задача | Priority | Status | Depends on |
|---|---|---:|---|---|
| R04-00 | Parallel release line + backlog setup | P0 | DONE | — |
| R04-01 | MOEX market identity + quote semantics contract | P0 | DONE | R04-00 |
| R04-02 | Read-only MOEX ISS provider/reference client | P1 | DONE | R04-01 |
| R04-03 | Instrument market mapping storage/API | P1 | DONE | R04-01 |
| R04-04 | Quote refresh preview API | P1 | DONE | R04-02, R04-03 |
| R04-05 | Mapping + quote preview UI | P1 | DONE | R04-04 |
| R04-05A | Provider-neutral market identity refactor | P0 | DONE | ADR 0010, R04-03…05 |
| R04-05B | T-Invest read-only production market-data adapter | P0 | DONE | R04-05A |
| R04-AUDIT-01 | Independent pre-apply architecture/correctness checkpoint | P0 | DONE | R04-05B |
| R04-05C | T-Invest mapping integrity hardening | P0 | DONE | R04-AUDIT-01 |
| R04-05D | Owner live read-only T-Invest probe + sanitized official fixture | P0 | DONE | R04-05C |
| R04-06 | Explicit selective apply + immutable provenance | P0 | DONE | R04-05D + maintenance forward-port + post-merge verification |
| R04-07 | Partial/stale/unavailable/manual fallback UX polish | P1 | DONE | R04-06 |
| R04-08 | Regression matrix + Windows/network smoke | P1 | DONE | R04-07 |
| R04-09 | 0.4 release gate + docs/metadata | P1 | DONE / TAGGED | R04-00…R04-08 |

`R04-AUDIT-01` reviewed exact `r04` implementation checkpoint `b8f50075cc1b91f2fae85003ab2b9a977984a5b6` and returned **READY WITH PRECONDITIONS** for R04-06. Those preconditions were closed by R04-05C, the live R04-05D gate, stable maintenance M03-01..03 forward-port, and exact integrated-r04 verification. R04-06 was then implemented from canonical baseline `e48183b04387452879f6e21e0f281df3da9460b8`, reviewed with four blocking follow-ups, and accepted after those follow-ups were closed. Final candidate `r04-06-grok` @ `540537385eb05d5793d807c2cb19b05428dfe3b8`; integrated via merge `40c1e9406f09d541df4281e51fc4473e22a4f997`. Issue #30 closed completed. R04-07 then started from canonical baseline `d397c4092e2e01911475726cc3e1246544441632`, passed independent inspection, and was accepted as candidate `r04-07-grok` @ `7f967be829be5e88283907a02461c4dbee0f7c84`; integrated via merge `61e6a1503b8b173920b6976ee5f04d4a2aef2f08`. Issue #31 closed completed. R04-08 then ran as release verification from canonical baseline `92b09628dc73646dde5d0855562431c0dd9872fd`; after independent multi-model review and four candidate iterations, final candidate `r04-08-grok` @ `ec9f56b1870402201ae863d80fd08cea758a4ac4` was accepted and integrated via merge `5a7d0add14d1699e72993af522d2e61f47a27144`. The detailed Grok/Codex/DeepSeek benchmark trail is preserved in `docs/reviews/2026-08-16-r04-08-model-benchmark.md`. R04-09 prepared the 0.4.0 release candidate on `r04-09-grok`. The later tagged identity is `v0.4.0` @ `5a29afb9870304faffb9c5911d4c23bcb2563349`. A GitHub Release object for 0.4.0 is not independently verified in this record.

---

# R04-05C. T-Invest mapping integrity hardening

**Priority:** P0  
**Status:** DONE

## Why

Current T-Invest mapping can persist `provider + instrument_uid` without mandatory provider verification and without carrying candidate ISIN. This is acceptable for read-only preview but is not strong enough before an external quote may mutate a monthly snapshot.

## Scope

- when discovery provides candidate ISIN, preserve it through the save request and hard-reject mismatch with local Instrument ISIN;
- if owner manually enters `instrument_uid` and local Instrument has ISIN but candidate ISIN is not available, accepted mapping must be provider-verified before persistence;
- T-Invest UI save path must use the safe verification contract rather than silently accepting local-only UID;
- malformed one candidate must not discard already valid discovery candidates when partial success is safe;
- preserve explicit owner choice; no auto-map;
- add negative tests for wrong UID/ISIN and verification failures;
- add complete mocked/injected `T-Invest mapping → quote preview` happy path.

## Must not do

- no snapshot mutation/apply/provenance;
- no background/startup network;
- no MOEX production fallback;
- no trading/order/account/transfer APIs;
- no real owner financial data in tests/logs.

## Acceptance

Accepted in `r04-05c-grok` @ `9c8b4bba2f58e79d6b5f012e5cade99393dbacae`; integrated into `r04` via merge `169079b7c1bebe95bf8cee3f12a2a58d36d3c1a4`. A T-Invest identity conflicting with a known local ISIN cannot become an accepted mapping, and a manual UID cannot bypass the required verification path before R04-06. Issue #28 closed after accepting review.

---

# R04-05D. Owner live read-only T-Invest probe + sanitized fixture

**Priority:** P0  
**Status:** DONE

## Scope

After R04-05C acceptance:

- explicit developer/owner invocation only;
- use only a **read-only** T-Invest token from ignored repository-root `.env`;
- probe only public/non-personal instrument and market-data endpoints already used by R04-05B;
- no Accounts/Operations/Orders/Sandbox/Transfer calls;
- confirm actual provider payload/date/Quotation/MoneyValue/candle-source semantics used by the adapter;
- confirm historical result never returns a quote later than requested target date;
- keep token and raw owner/account data out of Git/logs/reports;
- if a fixture is retained, store only sanitized public/non-personal representative payload needed for deterministic tests.

## Acceptance

Accepted on 2026-08-14 after a bounded authenticated owner-side read-only probe against the official T-Invest API shape. The probe called only `FindInstrument`, `GetInstrumentBy`, `BondBy`, `GetLastPrices`, and `GetCandles`; no account/trading/transfer methods were called. Quotation `units+nano`, `lastPriceType`, `candleSourceType`, bond MoneyValue and historical `price_date <= target_date` semantics matched the adapter. Deterministic fixture coverage remained sufficient and no raw live fixture/token was retained. Final candidate `r04-05d-grok` @ `08214f91994ddf3dfe7f91143d0d7fa343a2d711`; integrated via merge `e2a30e4d696de21e0b30bed46e9ffff78d042af5`. Issue #29 closed completed.

Owner troubleshooting observed during acceptance: on the owner's Windows environment the live call succeeded after disabling VPN; a previously rejected read-only token required reissue. These are troubleshooting observations only, documented in `docs/t-invest-market-data.md`, not application runtime requirements.

---

# R04-06. Explicit selective apply + immutable provenance

**Priority:** P0  
**Status:** DONE

## Required contract

1. Add `t_invest` as a real `PriceSource` and migrate the DB `price_source` constraint. Never encode T-Invest as `moex`.
2. Persist immutable **snapshot-scoped provider-neutral provenance** for every applied quote, including at minimum:
   - provider;
   - provider instrument id;
   - optional provider venue id;
   - quote kind/raw value/raw basis where applicable;
   - normalized price kopecks;
   - actual `price_date`;
   - `fetched_at_utc`;
   - target/reporting snapshot identity;
   - freshness classification.
3. Historical provenance must not be reconstructed later from mutable current mapping.
4. Existing editable-month guard remains authoritative. Closed month may preview, never apply without explicit reopen.
5. Apply only explicit owner-selected eligible rows. `stale` requires explicit per-row selection; it is never selected by default.
6. Production apply eligibility is T-Invest only for 0.4. MOEX reference mappings and `unmapped/excluded/unsupported/ambiguous/unavailable/network_error/malformed_response` cannot apply.
7. Frontend proposed money is never authoritative; backend obtains/normalizes the quote and recomputes existing position financial metrics through backend source-of-truth paths. NKD remains unchanged.
8. **Preview/apply consistency guard:** owner must never silently receive a different quote than the one just approved. Request may carry an expected preview fingerprint/value/date/identity as an optimistic guard, but not as financial truth. If refreshed server quote differs materially from the approved preview, reject with a deterministic `preview_changed`/conflict outcome and require a new preview.
9. **Atomic selected-set apply:** selected rows are one top-level transaction. Do not call the existing per-row `update_position_snapshot()` commit loop. Stage/flush selected changes and commit once; any failure rolls back the selected set.
10. Mapping edits after apply never rewrite historical price/provenance.

## Regression matrix

At minimum:

- one OK apply;
- multi-row atomic apply;
- one selected row fails → zero selected writes;
- closed month rejected;
- stale unselected by default + explicit stale selection path;
- quote changed since preview → conflict/no write;
- wrong provider/status rejected;
- T-Invest `price_source` persisted correctly;
- provenance immutable after mapping change;
- NKD unchanged;
- monetary metrics recomputed by backend;
- no background/startup network.

## Acceptance

Accepted on 2026-08-14 after reviewer follow-up closed four concrete blockers in the first implementation: provenance is append-only 1:N per snapshot; `t_invest` is reserved for the dedicated quote-apply path and generic CRUD cannot fabricate or silently corrupt it; stale selection is T-Invest-only; month clone carries valuation state but never fabricates apply-event provenance. Repeat apply appends history, `preview_changed` writes nothing, selected-set apply is atomic, closed months remain protected, and backend remains financial source of truth. Final candidate `r04-06-grok` @ `540537385eb05d5793d807c2cb19b05428dfe3b8`; integrated via merge `40c1e9406f09d541df4281e51fc4473e22a4f997`. Issue #30 closed completed.

---

# R04-07. Failure/manual fallback UX

**Priority:** P1  
**Status:** DONE

Sanitized provider failure reason/message contract distinguishes missing token, external provider/network failure, unavailable/unsupported/malformed/unmapped/excluded/ambiguous rows, and local browser→Hermes network failure without exposing raw upstream diagnostics. Mixed-success preview preserves usable good rows; stale T-Invest remains explicit-only while stale MOEX/reference rows are non-applicable. `preview_changed` invalidates the old preview/apply state and requires a fresh explicit preview; no auto-apply or background retry was introduced. Manual fallback remains available only in editable months and preserves R04-06 source/provenance rules; closed months stay immutable.

Accepted candidate `r04-07-grok` @ `7f967be829be5e88283907a02461c4dbee0f7c84` from exact baseline `d397c4092e2e01911475726cc3e1246544441632`; integrated via two-parent merge `61e6a1503b8b173920b6976ee5f04d4a2aef2f08`. Issue #31 closed completed after independent inspection.

---

# R04-08. Regression + Windows/network smoke

**Priority:** P1  
**Status:** DONE

Release-verification task only; no feature development. Final accepted verification proves the 0.4 mapping/preview/apply/provenance/failure/stale/closed-month/source-spoofing/M03 invariants, migration safety, Windows production runtime boundary and explicit-only network behavior.

Acceptance evidence includes:

- full backend `771 passed` and full frontend `230 passed / 40 files` on the final worker run;
- Alembic stable-copy upgrade through 0026 with existing snapshots preserved and no fabricated T-Invest/provenance;
- 0026 downgrade fail-closed when any append-only quote provenance exists, including a manual snapshot with historical T-Invest provenance;
- isolated cold-import/startup network guard installed before application imports, with a guard self-test proving outbound socket/HTTP attempts are caught;
- canonical Windows `scripts/start-local.ps1` smoke while the stack is live, proving the only TCP listener on port 8000 is `127.0.0.1`, followed by clean shutdown and no remaining listener;
- Windows PowerShell 5.1 regression coverage for repository paths containing both spaces and Cyrillic/non-ASCII characters, including explicit UTF-8 BOM handling;
- no live T-Invest call, no private data/token output, no background refresh/polling/retry.

Initial candidate `07dc2e12502196e9e5f4e7a64234b6d010d45e12` was independently challenged rather than accepted from worker evidence. Follow-ups were `0da5980447947ef5dac69b28e2b581bc876d2026`, `c68ca82b877e2085dbc57bdf7ce27ac845ec3601`, and final `ec9f56b1870402201ae863d80fd08cea758a4ac4`. Final candidate received independent ACCEPT from Codex GPT-5.6 Sol High and DeepSeek V4 Pro plus integrator read-back; detailed comparison including the earlier DeepSeek V4 Flash false ACCEPT is preserved in `docs/reviews/2026-08-16-r04-08-model-benchmark.md`.

Final candidate `r04-08-grok` @ `ec9f56b1870402201ae863d80fd08cea758a4ac4`; integrated via two-parent merge `5a7d0add14d1699e72993af522d2e61f47a27144`. Issue #32 is the canonical task record.

---

# R04-09. 0.4 release gate + docs/metadata — historical

**Priority:** P1  
**Status:** DONE / TAGGED (historical)

R04-09 was the release gate, not feature development. It prepared the exact 0.4.0 candidate and did not itself integrate `r04 → main`, create `v0.4.0`, or publish a GitHub Release. The later tagged identity is `v0.4.0` @ `5a29afb9870304faffb9c5911d4c23bcb2563349`.

## Maintenance reconciliation

Independently verified against fetched remotes:

- `origin/r04` = `0fbe819f37d52e1f04e59fa0f8f6a3a85f5bbf5a`
- `origin/main` = `1bf28bfdbec2c9c6d495a1de449bce2f541be13e`
- last accepted `main → r04` forward-port is two-parent merge `4b2d249a6a175bf4009ec1785dbded961a2b2fa9` (`d275b2e` + `1bf28bf`)
- `origin/main` is an ancestor of `origin/r04`
- no commits exist on `origin/main` after that forward-port

No no-op merge was created.

## Release metadata

Canonical runtime/package version is `0.4.0` in `__version__`, backend `pyproject.toml` / `uv.lock`, frontend `package.json` / root `package-lock.json`, `/api/health`, CHANGELOG and README. Product semantics were not changed.

## Deterministic verification

Recorded on the R04-09 candidate after metadata sync. Live T-Invest was not run.

| Check | Outcome |
|---|---|
| Full backend `pytest -q` | **771 passed** |
| Backend Ruff check | pass |
| Backend Ruff format check | pass (246 files) |
| Full frontend Vitest | **230 passed / 40 files** |
| Frontend Biome lint | pass (149 files) |
| Frontend `tsc -b && vite build` | pass |
| `git diff --check` | pass |
| `privacy_check.py` | PASS (462 tracked files) |
| Alembic through `0026_t_invest_price_source_and_provenance` | pass (`test_migrations.py` + live smoke `0001`…`0026`) |
| Provenance downgrade fail-closed + cold-import network guard | pass (`test_r04_08_release_verification.py`) |
| Windows Cyrillic + spaced launcher path | pass (`test_r04_08_windows_launcher_path.py`) |
| Windows production smoke from `Рабочий стол` + spaces | pass: live listener only `127.0.0.1:8000`, then gone |

## Publication

Tagged identity, independently verified from the Git tag:

- `v0.4.0` @ `5a29afb9870304faffb9c5911d4c23bcb2563349`
- commit subject: `merge: release Hermes Finance 0.4.0`

A GitHub Release object for 0.4.0 is not independently verified here. Current `main` need not equal this tag.

---
