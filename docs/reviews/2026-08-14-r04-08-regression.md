# R04-08 release verification — 2026-08-14

## Verdict

**READY FOR INDEPENDENT INSPECTION.** This is worker evidence, not acceptance.

Exact candidate is recorded after the task-branch commit. Live T-Invest probe was not run.

## Exact refs

- Canonical baseline required by issue #32: `92b09628dc73646dde5d0855562431c0dd9872fd`
- Worker branch: `r04-08-grok`
- Exact candidate HEAD is the tip of `r04-08-grok` that contains this note; the worker completion report records the full SHA
- Ancestry: baseline is an ancestor of the candidate HEAD
- `main` and `r04` were not modified
- No merge, PR, tag, or release was performed by the worker

## What this task changed

Verification-only lock-in. No product feature, provider strategy, financial formula, or apply contract was redesigned.

Added:

- `backend/tests/test_r04_08_release_verification.py` — synthetic stable-schema `0023` copy upgrade through `0026_t_invest_price_source_and_provenance`; provenance table/index/constraints; `t_invest` downgrade fail-closed; import/page-load does not construct market clients; missing-token production preview makes no HTTP call
- `scripts/r04-08-windows-smoke.ps1` — isolated-temp-DB wrapper around the canonical `scripts/start-local.ps1 -ExitAfterReady` path; unsets the token environment variable without reading it
- this evidence note

No owner DB, `.env`, token, export, backup, or private seed was read or committed.

## Required regression coverage

Existing suites already owned the product matrix. R04-08 re-ran them in full and added the missing stable-copy / launcher / network-boundary locks.

| Requirement | Evidence |
|---|---|
| Mapping discovery/save/ISIN integrity | `test_instrument_mappings.py`, `test_instrument_mappings_api.py`, frontend `InstrumentMappingDialog.test.tsx` / `instrumentMappings.test.ts` |
| Quote preview and mixed-success | `test_quote_preview.py`, `test_quote_preview_api.py`, `test_quote_failure_ux.py`, `QuotePreviewPanel.test.tsx` |
| Selective apply, atomic rollback, `preview_changed`, append-only provenance | `test_quote_apply.py`, `test_quote_apply_api.py`, `MonthPositionsSection.test.tsx` |
| Sanitized failure / manual fallback UX | `test_quote_failure_ux.py`, `marketData.test.ts`, `QuotePreviewPanel.test.tsx`, `MonthPositionsSection.test.tsx` |
| Stale T-Invest explicit-only; stale MOEX/reference non-applicable | `test_quote_apply.py`, `QuotePreviewPanel.test.tsx` |
| Closed-month guards | quote preview/apply tests plus month workspace tests |
| Source spoofing / reserved `t_invest` / manual override | `test_quote_apply_api.py`, `MonthPositionsSection.test.tsx` |
| M03-01 loopback bind | `test_cli.py`, `test_settings.py`, Windows smoke bind `127.0.0.1:8000` |
| M03-02 signed realized loss | `test_investment_cash_flows.py` |
| M03-03 salary cardinality | `test_salary_cardinality.py` |

Targeted matrix re-run after the new tests: **119 passed**, 38 deselected.

## Migration

Synthetic stable-schema copy, not an owner database:

1. Alembic upgrade to `0023_passive_income_history_eligibility` (last stable 0.3 schema)
2. Seed closed + draft months and three snapshots with `manual` / `moex` / `alfa_pdf`
3. File-copy the DB
4. Upgrade the copy to `0026_t_invest_price_source_and_provenance`

Observed:

- original copy remains on `0023`
- snapshot fingerprints survive (ids, sources, kopecks, dates, notes)
- no `t_invest` rows are fabricated
- `instrument_market_mappings` is created empty (no auto-map from `moex_secid`)
- `position_quote_provenance` exists, is empty, and has `ix_position_quote_provenance_snapshot_id`
- snapshot `price_source` check now includes `t_invest` while existing values stay valid
- downgrade with a `t_invest` row fails closed and stays on `0026`

No private financial values were printed into this note.

## Windows runtime smoke

Canonical path: `scripts/r04-08-windows-smoke.ps1` → `scripts/start-local.ps1 -ExitAfterReady`.

Observed on this Windows worktree:

- isolated temp SQLite path; token env unset without reading
- frontend production build succeeded
- Alembic ran `0001` … `0026_t_invest_price_source_and_provenance`
- Uvicorn: `Uvicorn running on http://127.0.0.1:8000`
- `GET /api/health` 200
- `GET /api/months` 200
- `GET /` 200
- process stopped; port 8000 not left listening

Host `0.0.0.0` / `localhost` / `::1` remain rejected by settings/CLI tests. Port override `9001` remains accepted by `test_cli.py` / `test_settings.py`.

## Network boundary

Deterministic / mocked only. No live T-Invest call.

- import + health + months + page load do not construct `TInvestClient` / `MoexIssClient`
- FastAPI `on_startup` is empty; no background poller
- missing token preview uses injected forbidden HTTP transport and performs no request
- existing provider tests keep missing-token, timeout/`provider_network`, and sanitized messages distinct from local browser→Hermes failure
- mixed-success preview keeps good rows
- frontend does not call preview until the explicit owner button; no `refetchInterval`

Forbidden T-Invest APIs remain rejected by the probe allowlist tests: Accounts / Operations / Orders / Sandbox / Transfer.

## Privacy / secrets

- `scripts/privacy_check.py`: PASS on tracked files
- `.env` is ignored (`.gitignore:2:.env`) and was absent from this worktree
- `git ls-files` has `.env.example` only among env-like files
- `HERMES_FINANCE_T_INVEST_READ_ONLY_TOKEN` was not read or printed
- no private DB/export/backup/owner payload was opened
- owner-facing preview path sanitizes token-unavailable text and does not echo `t.` prefixes

## Local exact-candidate checks

| Check | Outcome |
|---|---|
| Full backend `pytest -q` | **767 passed** |
| Backend Ruff check | pass |
| Backend Ruff format check | pass (244 files) |
| Full frontend Vitest | **230 passed / 40 files** |
| Frontend Biome lint | pass (149 files) |
| Frontend `tsc -b && vite build` | pass |
| `git diff --check` | pass |
| Privacy check | pass |
| Windows production smoke | pass |
| Live T-Invest probe | **not run** (issue default) |

### Known non-blocking tooling noise

Tree-wide `npm run format-check` reports the established Windows CRLF normalization (`\r\n` → `\n`) across untouched frontend files. CI frontend format-check runs on Ubuntu and is not this local line-ending noise. No frontend product file was rewritten to chase it.

## CI

Repository workflow `.github/workflows/ci.yml` triggers on `push` to `main` and on `pull_request` only. Pushing `r04-08-grok` does not start GitHub Actions. Worker did not open a PR (owner permission required). Independent inspection can start CI by opening a PR against `r04` if desired.

Canonical local equivalent of the CI jobs was run on this Windows candidate: privacy, backend lint/format/tests, frontend lint/tests/build, and the Windows production smoke job.

## Scope / blockers

- No product defect requiring an R04-08 code fix was found
- No new stable-maintenance defect was split out
- R04-09 release gate is **not started**
