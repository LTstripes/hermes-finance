# PERF-H1 v3

The normative PERF-H1 v3 contract is intentionally kept in
`docs/PERFORMANCE_V1_RECONCILIATION_2026-09-06.md` so workers have one canonical
performance-roadmap source. This marker exists only to make repository search
for `PERF-H1 v3` resolve to that document.
