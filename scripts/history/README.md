# Historical release smoke scripts

This directory holds release-specific Windows verification scripts.

They are **not** the current production smoke path.

Canonical current production smoke:

- CI;
- [`scripts/start-local.ps1`](../start-local.ps1).

The active helper [`scripts/windows-powershell-file.ps1`](../windows-powershell-file.ps1) stays in `scripts/`. These archived wrappers are kept for historical and debug reference only.
