# Sketches

This directory contains historical design explorations.

It is not a UI source of truth. Production UI lives under `frontend/`.

Sketches may be consulted as design references during explicitly scoped redesign work. No sketch is automatically an implementation requirement.
